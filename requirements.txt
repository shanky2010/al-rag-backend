# IndustrialRAG — Render.com compatible requirements

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# Vector store (IndexIDMap requires faiss-cpu >= 1.7.4)
faiss-cpu==1.8.0
sentence-transformers==2.7.0

# Numpy — pinned for faiss compatibility
numpy>=1.24,<2.0

# Document parsing
pdfplumber==0.11.0
pandas==2.2.2
openpyxl==3.1.2
xlrd==2.0.1

# LLM — Anthropic is primary on Render (set ANTHROPIC_API_KEY env var)
anthropic>=0.26.0

# Optional: OpenAI fallback (set OPENAI_API_KEY env var)
# openai>=1.30.0

# OCR for scanned PDFs (optional — works if tesseract binary is installed)
pytesseract==0.3.13
Pillow==10.3.0

# HTTP client (used by Ollama fallback locally)
requests==2.32.0
