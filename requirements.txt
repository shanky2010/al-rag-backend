# IndustrialRAG — Render.com compatible requirements
# Versions unpinned where possible so pip picks pre-built wheels

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Vector store
faiss-cpu==1.12.0
sentence-transformers>=2.7.0

# Numpy
numpy>=1.24,<3.0

# Document parsing
pdfplumber>=0.11.0
pandas>=2.2.0
openpyxl>=3.1.0
xlrd>=2.0.0

# LLM — Anthropic is primary on Render (set ANTHROPIC_API_KEY env var)
anthropic>=0.26.0

# OCR for scanned PDFs
pytesseract>=0.3.10
Pillow>=10.0.0

# HTTP client
requests>=2.32.0
